#pragma once

/**
 * You don't need to modify this code.
 *
 * This is a class that contains both a row and a column. It doesn't actually
 * *do* anything, it only holds these two values so that some other code can
 * obtain it.
 */
class RowColumn
{
public:
    unsigned int row;
    unsigned int column;
};

